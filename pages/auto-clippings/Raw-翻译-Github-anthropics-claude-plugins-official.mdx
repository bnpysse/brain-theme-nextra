---
title: "_anthropics_claude-plugins-official_全翻译"
tags: ["Github_Trending", "全文翻译"]
---

# Claude Code Plugins Directory
Claude Code插件目录

A curated directory of high-quality plugins for Claude Code.
精心筛选的Claude Code高质量插件目录

&gt; **⚠️ Important:** Make sure you trust a plugin before installing, updating, or using it. Anthropic does not control what MCP servers, files, or other software are included in plugins and cannot verify that they will work as intended or that they won't change. See each plugin's homepage for more information.
&gt; **⚠️ 重要提示:** 安装、更新或使用插件前请确保信任该插件。Anthropic不控制插件包含的MCP服务器、文件或其他软件，也无法验证其是否按预期工作或是否会发生变更。更多信息请查看各插件主页。

## Structure
## 目录结构

- **`/plugins`** - Internal plugins developed and maintained by Anthropic
- **`/plugins`** - 由Anthropic开发和维护的内部插件
- **`/external_plugins`** - Third-party plugins from partners and the community
- **`/external_plugins`** - 来自合作伙伴和社区的第三方插件

## Installation
## 安装说明

Plugins can be installed directly from this marketplace via Claude Code's plugin system.
可通过Claude Code插件系统直接从本市场安装插件。

To install, run `/plugin install {plugin-name}@claude-plugins-official`
安装命令：`/plugin install {插件名称}@claude-plugins-official`

or browse for the plugin in `/plugin &gt; Discover`
或在`/plugin &gt; Discover`中浏览插件

## Contributing
## 贡献指南

### Internal Plugins
### 内部插件

Internal plugins are developed by Anthropic team members. See `/plugins/example-plugin` for a reference implementation.
内部插件由Anthropic团队成员开发。参考实现见`/plugins/example-plugin`

### External Plugins
### 外部插件

Third-party partners can submit plugins for inclusion in the marketplace. External plugins must meet quality and security standards for approval. To submit a new plugin, use the [plugin directory submission form](https://clau.de/plugin-directory-submission).
第三方合作伙伴可提交插件申请上架。外部插件需满足质量和安全标准才能获批。提交新插件请使用[插件目录提交表单](https://clau.de/plugin-directory-submission)

## Plugin Structure
## 插件结构

Each plugin follows a standard structure:
每个插件遵循标准结构：

```
plugin-name/
├── .claude-plugin/
│   └── plugin.json      # Plugin metadata (required)
├── .mcp.json            # MCP server configuration (optional)
├── commands/            # Slash commands (optional)
├── agents/              # Agent definitions (optional)
├── skills/              # Skill definitions (optional)
└── README.md            # Documentation
```

## Plugin names are immutable
## 插件名称不可更改

The `name` field in a marketplace entry is an **immutable slug**. Once a plugin has been published, its `name` must not change — users have it installed under that slug, and renaming it breaks their install with a `plugin-not-found` error.
市场条目中的`name`字段是**不可变的标识符**。插件发布后其`name`不得更改——用户已使用该标识符安装插件，重命名会导致安装中断并出现`plugin-not-found`错误。

- To change how a plugin is labeled in the UI, set or update `displayName` instead.
- 如需修改UI中显示的标签，请设置或更新`displayName`
- If a rename is genuinely unavoidable, add an entry to the top-level `renames` map in `.claude-plugin/marketplace.json` so existing installs auto-migrate:
- 若必须重命名，请在`.claude-plugin/marketplace.json`的顶层`renames`映射中添加条目以实现现有安装自动迁移：

```json
"renames": {
  "old-name": "new-name"
}
```

The Claude Code plugin loader reads this map and transparently rewrites the old slug to the new one on the user's next sync.
Claude Code插件加载器会读取该映射，在用户下次同步时透明地将旧标识符重写为新标识符。

## Skill-bundle plugins
## 技能包插件

When a plugin's source repository ships skills (`SKILL.md` files) without a `.claude-plugin/plugin.json` manifest, the marketplace entry can declare the skills directly using `strict: false` and an explicit `skills` array.
当插件源码仓库包含技能(`SKILL.md`文件)但无`.claude-plugin/plugin.json`清单时，市场条目可通过`strict: false`和显式`skills`数组直接声明技能。

```json
{
  "name": "example-bundle",
  "description": "Brief description of the bundled skills.",
  "author": { "name": "Author Name" },
  "category": "development",
  "source": {
    "source": "git-subdir",
    "url": "https://github.com/example-org/sdk.git",
    "path": "packages/agent-skills",
    "ref": "main",
    "sha": "<commit sha>"
  },
  "strict": false,
  "skills": [
    "./skill-a",
    "./skill-b",
    "./skill-c"
  ],
  "homepage": "https://github.com/example-org/sdk"
}
```

Each path in `skills` is relative to `source.path` and points at a directory containing a `SKILL.md`. Paths can reach deeper than a single level — for example, `["./libA/skill-1", "./libB/skill-2"]` exposes a curated subset across multiple library subdirectories. Each skill is registered as `&lt;plugin-name&gt;:&lt;skill-name&gt;` in Claude Code.
`skills`中的每个路径相对于`source.path`，指向包含`SKILL.md`的目录。路径可跨多级目录——例如`["./libA/skill-1", "./libB/skill-2"]`可暴露跨多个库子目录的精选子集。每个技能在Claude Code中注册为`&lt;插件名称&gt;:&lt;技能名称&gt;`

For the underlying schema, see [Strict mode](https://code.claude.com/docs/en/plugin-marketplaces) in the marketplace documentation.
底层模式请参阅市场文档中的[严格模式](https://code.claude.com/docs/en/plugin-marketplaces)

## License
## 许可证

Please see each linked plugin for the relevant LICENSE file.
各插件的许可证文件请查看对应链接

## Documentation
## 文档

For more information on developing Claude Code plugins, see the [official documentation](https://code.claude.com/docs/en/plugins).
更多Claude Code插件开发信息，请参阅[官方文档](https://code.claude.com/docs/en/plugins)